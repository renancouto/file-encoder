/*globals describe, it*/
'use strict'

/**
 * dependencies
 */
import assert from 'assert'

/**
 * tests
 */
describe('File encoding tests', () => {
  it('convert the file', () => {
    assert.equal(true, false)
  })
})
